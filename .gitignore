# Binaries and build artifacts
glt
*.o
*.exe
*.out
*.d

# Local test renders (keep curated screenshots/ in git)
/tmp/
output.ppm
output.png
test.ppm

# Agent loop logs (appended on every run, not part of the project)
logs/

# Agent scaffolding (local automation, not part of the public project)
loop.sh

# Editor / OS noise
.vscode/
.idea/
*~
.DS_Store
